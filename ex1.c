#include <stdio.h>
/* multiline comment
  still a multiline comment
*/
int main(int argc, char *argv[])
{
  int distance = 100;
  int km = 30;
  //single line comment

  printf("You are %d miles away or %d kilometers.\n", distance, km);

  return 0;
}